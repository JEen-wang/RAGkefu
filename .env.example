# 复制为 .env 后按需修改（.env 已被 gitignore，勿提交密钥）
# cp .env.example .env

APP_NAME=RAGkefu
APP_ENV=local
APP_VERSION=0.1.0
DEBUG=true
API_PREFIX=/v1

# 后续步骤启用（当前仅占位，S04 不实际连接）
DATABASE_URL=postgresql+asyncpg://ragkefu:ragkefu@localhost:5432/ragkefu
REDIS_URL=redis://localhost:6379/0
CHROMA_URL=http://localhost:8001
