# RAGkefu 运行时基线（S03–S10 及通用基础设施）
# 建议 Python >= 3.11（本地若为 3.9，请用 pyenv/conda 或后续 Docker 镜像）

# Web
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# Config / validation
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# HTTP client (tests, internal calls, OpenAI-compatible APIs later)
httpx>=0.27.0,<1.0.0

# Observability (metrics endpoint; LangSmith 见 requirements-rag.txt)
prometheus-client>=0.21.0,<1.0.0

# Database (async) — 精确查询路径会用到
SQLAlchemy>=2.0.36,<3.0.0
asyncpg>=0.30.0,<1.0.0
greenlet>=3.0.0,<4.0.0
